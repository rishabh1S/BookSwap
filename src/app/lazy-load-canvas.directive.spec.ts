import { LazyLoadCanvasDirective } from './lazy-load-canvas.directive';

describe('LazyLoadCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
